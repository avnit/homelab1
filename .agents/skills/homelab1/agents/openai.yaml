interface:
  display_name: "Homelab1"
  short_description: "Repo-specific patterns and workflows for homelab1"
  default_prompt: "Use the homelab1 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true